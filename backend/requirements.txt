# FastAPI
fastapi==0.109.0
uvicorn[standard]==0.27.0
gunicorn==21.2.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9

# Redis & Celery (optional — only needed with ENABLE_BACKGROUND_TASKS=true)
redis==5.0.1
celery==5.3.6

# Error Tracking
sentry-sdk[fastapi]==1.40.0

# GitHub
PyGithub==2.1.1
PyJWT==2.8.0
cryptography==42.0.0

# AI/LLM
openai==1.10.0
anthropic==0.18.1
requests==2.31.0

# Code Analysis & AST Parsing (lightweight — no CUDA needed)
tree-sitter==0.21.3
tree-sitter-python==0.21.0
tree-sitter-javascript==0.21.4
tree-sitter-typescript==0.21.2

# Semantic Search & Embeddings (Phase 2) — CPU only (PyTorch included but no CUDA)
sentence-transformers==2.3.1
pgvector==0.2.4
numpy==1.26.3

# Code Sandbox (Phase 2) — only works where Docker daemon is available
# docker==7.0.0

# Linting/Analysis
pylint==3.0.3
flake8==7.0.0
bandit==1.7.6

# Utilities
python-multipart==0.0.6

# Email (Phase 3A)
python-dotenv==1.0.0
jinja2==3.1.3
aiosmtplib==3.0.1
email-validator==2.1.0
httpx==0.26.0
tenacity==8.2.3

# Auth
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# Rate Limiting
slowapi==0.1.9

# Billing (Phase 3B)
stripe==8.0.0
